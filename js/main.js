// Variables ===================================================================

// Initialization ==============================================================

// Run functions when document is ready
$(document).ready(function () {
    $("#justified-gallery").justifiedGallery({
        rowHeight: 180,
        margins: 10,
    });
});

// Functions ===================================================================